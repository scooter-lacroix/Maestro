---
description: "Maestro: initialize project (setup)"
argument-hint: "[no args]"
---

You are executing the Maestro framework command **setup** in Codex CLI.

1. Read and follow the canonical protocol at:
   `__MAESTRO_HOME__/integrations/commands/maestro:setup.md`

2. Tooling differences:
   - If the protocol references Claude Code–specific tools (e.g., `AskUserQuestion`), perform the equivalent interaction using Codex CLI’s normal chat/UX.
   - Preserve the protocol’s intent: ask the same questions, enforce the same gates, and validate every step.

