description = "Maestro: revert track/phase/task (revert)"
prompt = """
You are executing the Maestro framework command **revert** in Qwen Code.

Load the canonical protocol and follow it precisely:

@{__MAESTRO_HOME__/integrations/commands/maestro:revert.md}

The user’s arguments ({{args}}) scope the revert.
"""

